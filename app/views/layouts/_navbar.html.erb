<% if user_signed_in? %>
<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">INSS</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to t("views.nav.item"), proponents_path, class: "nav-link #{'active' if current_page?(proponents_path)}" %>
        </li>
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <%= current_user.email %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <%= link_to(t("views.nav.user.profile"), edit_user_registration_path, class: "dropdown-item") %>
            <li><hr class="dropdown-divider"></li>
            <%= button_to(t("views.nav.user.logout"), destroy_user_session_path, method: :delete, class: "dropdown-item") %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
<% end %>
